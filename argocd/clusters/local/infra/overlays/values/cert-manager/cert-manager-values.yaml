apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
spec:
  source:
    helm:
      values: |
        podLabels:
          environment: local
    targetRevision: "v1.6.1"
