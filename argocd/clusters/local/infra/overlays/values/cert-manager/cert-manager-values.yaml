apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager-local
spec:
  source:
    helm:
      values: |
        podLabels:
          environment: local
    targetRevision: "v1.6.1"
